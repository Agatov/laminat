%legend
  Текстуры

  %a.btn{href: new_admin_texture_path, style: 'float: right'} Добавить

%table.table
  %thead
    %tr
      %th
        Название
      %th
        = '...'
  %tbody
    - @textures.each do |texture|
      %tr
        %td
          = texture.name
        %td
          = link_to 'редактировать', edit_admin_texture_path(texture)
          = link_to 'удалить', admin_texture_path(texture), method: :delete